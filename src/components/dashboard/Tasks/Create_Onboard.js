import styles from '../../../styles/components/dashboard/tasks/Create.module.scss';


function Create_Onboard (props) {

    return (
        <main className={styles.main}>
            <h2>Create Onboard Task</h2>
            <p>These tasks will trigger when a new employee joins the Company.</p>
        </main>
    )
}

export default Create_Onboard;